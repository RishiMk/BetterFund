# ======================
# Application Identity
# ======================
spring.application.name=campaign-service
server.port=8082

# ======================
# MySQL Database Config
# ======================
spring.datasource.url=jdbc:mysql://localhost:3306/p13_crowdfunding_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Rushimk@1506
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ======================
# Hibernate / JPA Config
# ======================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ======================
# Eureka Client Config
# ======================
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# ======================
# Multipart File Upload (optional)
# ======================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
